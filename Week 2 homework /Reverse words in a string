class Solution(object):
    def reverseWords(self, s):
        """
        :type s: str
        :rtype: str
        """
        s.strip()
        for ch in s:
            if s==" " and s.next==" ":
                s=""
        lst=s.split()
        lst.reverse()
        newlst=" ".join(lst)
        return newlst
